import Link from "next/link";
import * as styles from "./post-category-list.css"


export default function PostCategoryList(
    { prev, next } : {prev: string | null, next: string | null}
) {

    return (
        <div className={styles.container}>

        </div>
    );
}