import { style } from '@vanilla-extract/css';

export const container = style({
    position: 'absolute',
    width : '50%',
    left: '50%',
    marginLeft: '-25%',
    textAlign: 'center',
    bottom: '5px',
});
